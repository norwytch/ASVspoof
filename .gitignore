# Data — never commit the corpora
data/asvspoof2021_LA/
data/asvspoof5/
data/musan/
*.flac
*.wav
*.mp3

# Vendored baseline code + downloaded model weights (reproducible: git clone +
# gdown; see src/ssl_aasist.py). Too large / not ours to commit.
third_party/
weights/

# Results artifacts: commit the small deliverables (figures + summary CSVs),
# ignore the large/regenerable corpora-derived caches.
results/figures/*
!results/figures/.gitkeep
!results/figures/*.png
results/embeddings/
results/embeddings_ft/
results/scores/
*.npy
*.npz
results/*.jsonl
results/*.pt
results/*.log
checkpoints/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
audio_deepfake_robustness_proposal.md
anticipated-questions.md   # private interview prep, not portfolio-facing

# OS
.DS_Store
