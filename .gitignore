# Data — never commit the corpora
data/asvspoof2021_LA/
data/asvspoof5/
data/musan/
*.flac
*.wav
*.mp3

# Vendored baseline code + downloaded model weights (reproducible: git clone +
# gdown; see src/ssl_aasist.py). Too large / not ours to commit.
third_party/
weights/

# Results artifacts (keep dirs via .gitkeep, ignore contents)
results/figures/*
!results/figures/.gitkeep
results/*.csv
results/*.jsonl
results/*.pt
checkpoints/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
audio_deepfake_robustness_proposal.md
report.md

# OS
.DS_Store
