# Data — never commit the corpora
data/asvspoof2021_LA/
data/asvspoof5/
data/musan/
*.flac
*.wav
*.mp3

# Vendored baseline code + downloaded model weights (reproducible: git clone +
# gdown; see src/ssl_aasist.py). Too large / not ours to commit.
third_party/
weights/

# Results artifacts: commit the small deliverables (figures + summary CSVs),
# ignore the large/regenerable corpora-derived caches.
results/figures/*
!results/figures/.gitkeep
!results/figures/*.png
results/embeddings*/
results/scores/
*.npy
*.npz
*.tar
results/*.jsonl
results/*.pt
results/*.log
checkpoints/

# stale pre-fix subset CSVs, superseded by the *_full versions; kept local only
results/results.csv
results/per_attack_eer.csv
results/codec_eer.csv
results/loao.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
audio_deepfake_robustness_proposal.md
# private interview prep, not portfolio-facing
anticipated-questions.md
# redundant with report.md + contains interview-prep asides; kept local only
walkthrough.md
# internal orientation/handoff docs — kept local only
CLAUDE.md
next-session.md
handoff.md
files.zip

# OS
.DS_Store
